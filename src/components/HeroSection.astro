---
import { Image } from "astro:assets";
import HeroImage from "../assets/heroimage.jpg";
import HeroSocials from "./HeroSocials.astro";
---

<section id="about" class="flex flex-col md:flex-row gap-4 mx-4">
  <div class="flex flex-col gap-2 md:w-2/3 w-full">
    <div class="section-bg-color flex grow app-rounded-default">
      <div class="flex flex-col gap-4 m-auto max-w-2xl">
        <h1 class="text-4xl font-bold">
          I'm <span class="text-primary bg-black px-2">Tone Gabes</span>. I build interactive platforms with Laravel and JavaScript
        </h1>
        <p class="text-gray-700 text-lg mt-6">
          Full‑stack developer with 10+ years building digital platforms end‑to‑end. I focus on clean architecture, delightful interfaces, and content that delivers impact.
        </p>
      </div>
    </div>

    <HeroSocials />
  </div>

  <div class="aspect-square md:w-1/3 w-full">
    <Image
      src={HeroImage}
      alt="Tone Gabes headshot"
      class="w-full h-full object-cover app-rounded-default"
      loading="eager"
    />
  </div>
</section>
