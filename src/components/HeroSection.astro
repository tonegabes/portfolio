---
import HeroSocials from "./HeroSocials.astro";
---

<!-- Hero Section Épica -->
<section class="section-epic relative overflow-hidden">
  <div class="section-content relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Badge de Status -->
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-green-50 border border-green-200 rounded-full text-green-700 text-sm font-medium mb-8 animate-bounce">
        <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
        Disponível para novos projetos
      </div>

      <!-- Título Principal Épico -->
      <h1 class="text-hero mb-6 leading-tight text-gray-900">
        Criando
        <span class="gradient-text relative">
          experiências digitais
          <!-- Underline animado -->
          <svg class="absolute -bottom-2 left-0 w-full h-3 opacity-30" viewBox="0 0 100 10" preserveAspectRatio="none" style="color: var(--primary);">
            <path d="M0,5 Q25,0 50,5 T100,5" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round"/>
          </svg>
        </span>
        <br />
        que <span style="color: var(--accent);">inspiram</span> e <span style="color: var(--primary);">convertem</span>
      </h1>

      <!-- Subtítulo com Storytelling -->
      <div class="text-xl md:text-2xl mb-8 leading-relaxed max-w-3xl mx-auto" style="color: var(--neutral-600);">
        <p class="mb-4">
          <span class="font-semibold" style="color: var(--neutral-800);">Senior Full-Stack Developer</span> com 10+ anos transformando ideias em
          <span class="relative">
            produtos digitais épicos
            <span class="absolute inset-x-0 bottom-0 h-0.5" style="background: linear-gradient(to right, var(--primary), var(--accent));"></span>
          </span>
        </p>
        <p class="text-lg" style="color: var(--neutral-500);">
          De <strong>Laravel</strong> no backend até <strong>React/Vue</strong> no frontend,
          criando arquiteturas robustas com interfaces que seus usuários vão <em>amar</em> usar.
        </p>
      </div>

      <!-- Stats Impressionantes -->
      <div class="flex flex-wrap justify-center gap-8 mb-12">
        <div class="text-center">
          <div class="text-3xl font-bold" style="color: var(--primary);">10+</div>
          <div class="text-sm font-medium" style="color: var(--neutral-500);">Anos de Experiência</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold" style="color: var(--accent);">50+</div>
          <div class="text-sm font-medium" style="color: var(--neutral-500);">Projetos Entregues</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold text-green-500">99%</div>
          <div class="text-sm font-medium" style="color: var(--neutral-500);">Clientes Satisfeitos</div>
        </div>
      </div>

      <!-- CTAs Épicos -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12">
        <a href="/projects" class="btn-primary group">
          <span>Ver Meus Projetos</span>
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>

        <a href="/contact" class="btn-secondary group">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
          </svg>
          <span>Vamos Conversar</span>
        </a>
      </div>

      <!-- Redes Sociais Redesenhadas -->
      <HeroSocials />

      <!-- Scroll Indicator -->
      <div class="mt-16 flex justify-center">
        <div class="animate-bounce">
          <svg class="w-6 h-6 text-neutral-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
          </svg>
        </div>
      </div>
    </div>
  </div>

  <!-- Elementos de Background Adicionais -->
  <div class="absolute top-1/4 left-0 w-32 h-32 bg-primary-500/5 rounded-full blur-2xl"></div>
  <div class="absolute bottom-1/4 right-0 w-40 h-40 bg-accent-500/5 rounded-full blur-2xl"></div>

  <!-- Floating Elements -->
  <div class="absolute top-20 right-20 hidden lg:block animate-float">
    <div class="w-3 h-3 bg-primary-500 rounded-full opacity-60"></div>
  </div>
  <div class="absolute top-40 left-20 hidden lg:block animate-float-delayed">
    <div class="w-2 h-2 bg-accent-500 rounded-full opacity-40"></div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float 3s ease-in-out infinite 1.5s;
  }
</style>
